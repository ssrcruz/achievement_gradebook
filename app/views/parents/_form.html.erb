<%= form_for(@parent) do |f| %>
  <% if @parent.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@parent.errors.count, "error") %> prohibited this item from being saved:</h2>

    <ul>
      <% @parent.errors.full_messages.each do |message| %>
      <li><%= message %></li>
      <% end %>
    </ul>
  </div>
  <% end %>


  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :email %><br>
    <%= f.text_field :email %>
  </div>
  <div class="field">
    <%= f.label :password %><br>
    <%= f.text_field :password %>
  </div>
  <div class="field">
    <%= f.label "Guardian of:" %><br>
    <%= f.select :child_name, options_for_select(Student.get_all_children), include_blank: true %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
  <%= link_to 'Delete this item', @parent, method: :delete, data: { confirm: 'Are you sure?' } %>

  <% end %>
